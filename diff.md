# misskey-juiceの独自機能・本家との差分

本家Misskeyから追加・変更した、このフォーク独自の機能一覧。詳細な変更履歴は[CHANGELOG.md](./CHANGELOG.md)の`Unreleased`と`-juice`が付いたバージョンを参照。サーバー上では`/about-juice`ページでも主要な機能の一覧を確認できる。

## 承認式新規登録

- 新規登録に理由の入力を必須にし、Moderator/adminが承認するまでサインインできない「承認式新規登録」を追加
- 招待コードが有効な場合は承認をバイパス(招待した時点でモデレーターの信任があるため)。承認式登録が有効なサーバーでは招待コード自体も必須から任意に変更
- メールアドレスを収集しないサーバーでも、申請時に確認コードを発行して`/signup-check`から審査状況(審査待ち・承認・却下)を確認できるように。1端末から複数アカウント分申請してもそれぞれ個別に追跡できる。審査待ち画面・サインイン時の未承認エラーからも直接この確認ページへ移動できる
- 却下時に理由を記載可能。却下されたメールアドレス宛てのメール・審査状況確認画面(引換コード)の両方で理由を確認できる
- コントロールパネルに承認待ちユーザーの一覧・承認・却下画面を追加。審査待ちだけでなく承認済み/却下済みの履歴も閲覧可能で、誰がいつ審査したかも表示(モデレーターの特定は管理画面内のみで、申請者本人には非公開)。却下されたアカウントは物理削除される仕様のため、申請時点のユーザー名・登録理由をスナップショットとして保持し、削除後も履歴から確認できる
- 承認式登録が有効なサーバーでは、訪問者向けトップページ・登録前のルール確認画面・新規登録フォームに案内を表示

## 絵文字・アバターデコレーション申請

- 一般ユーザーが画像・名前・カテゴリ・タグ・ライセンス・センシティブ・ローカルのみを指定してカスタム絵文字を申請できる新規ページを追加。申請フォームは本家の管理画面「絵文字の登録」ダイアログに準拠したUI。アバターデコレーションについても同様の申請ページを追加(画像・名前・カテゴリ・説明を指定)
- コントロールパネルに絵文字・アバターデコレーションそれぞれの申請一覧・承認・却下画面を追加。承認時は既存の`CustomEmojiService`/アバターデコレーション管理にそのまま接続して登録、却下時は理由をメールで通知(受け取るかどうかはユーザーごとに設定可能)。承認/却下結果はメールに加えてアプリ内通知でも受け取れる(設定画面から個別にON/OFF可能)
- 審査一覧は審査待ちだけでなく承認済み/却下済みの履歴も閲覧可能で、誰がいつ審査したかも表示。自分の申請一覧では申請した画像のサムネイル・設定したタグ(別名)・センシティブ・ローカル限定の各項目も確認できる
- 新しい絵文字/デコレーションとしてではなく、自分の過去の承認済み申請から作られた既存の絵文字/デコレーションの画像だけを差し替えられる「差し替え申請」を追加。対象は申請者自身が作成したものに限られ、名前・カテゴリ等のメタデータは変更されない。審査画面では差し替え前後の画像を比較表示
- 複数の画像をまとめて選択すると、ロールポリシーで設定できる同時申請数の上限まで1回の操作でまとめて申請できる(hCaptcha等のcaptchaトークンは使い切りのため、キャプチャ検証1回でバッチ作成する専用API)
- ロールの権限設定に、1ユーザーが同時に出せる未審査の申請数の上限(`emojiRequestLimit`/`avatarDecorationRequestLimit`、既定値3)を追加
- アバターデコレーション申請フォーム・審査画面に、装着ダイアログと同じ角度・位置(X/Y)・反転の調整UIを備えたプレビュー機能を追加。明背景・暗背景それぞれでの見え方も並べて確認できる(プレビュー確認用のみで、申請データ自体には影響しない)。絵文字申請フォームにも、申請中の画像をノートにリアクションした場合の見た目のプレビューを追加
- 各申請の承認/却下権限を、モデレーターに限らずロール単位で個別に付与可能(`canApproveEmojiRequests`/`canApproveAvatarDecorationRequests`)。権限を持つ非モデレーターは、コントロールパネルを経由せず「ツール」メニューから直接各審査画面へ到達できる(カスタム絵文字管理・アバターデコレーション管理と同じ方式)
- 機能自体の有効・無効はコントロールパネルのJUICE設定から切り替え可能(既定は無効)

## リアクション

- ノートに既についている他人のリアクションをクリックして自分も同じリアクションを付ける「相乗り」機能を実装(ロール制限・センシティブ・ローカル限定といった権限チェックに対応)。リモートサーバーのカスタム絵文字によるリアクションへの相乗りは、他サーバーの絵文字画像を著作権者の許諾なく使用することになりうるため、コントロールパネルのJUICE設定でオプトイン(既定は無効)
- リアクション文字列(`:name@host:`)でリモートホストのカスタム絵文字を指定した場合、ローカルユーザーからのリアクションに限り指定ホスト・ローカル・ノート投稿者のホストの順で絵文字を探して認識(リモートから受信したリアクションは、なりすまし防止のため従来通りリアクションした本人のホストのみを対象)。JUICE設定で無効化した場合は本人のホスト(=ローカル)の絵文字のみを対象にする従来の挙動のまま
- リアクションの絵文字情報(ライセンス等)を確認できる「情報」メニューを、ローカルのカスタム絵文字だけでなくリモートサーバーのカスタム絵文字によるリアクションでも利用可能に
- 検索ページに、自分が付けたリアクションでノートを絞り込む専用タブ「リアクション」を追加。「何かしらリアクションした投稿」または特定のリアクションを選んでの絞り込みに、ノート検索と同じ高度な検索オプション・日時範囲・検索範囲・URL/@ユーザー/#タグの自動リンクも組み合わせ可能(プライバシー上、自分自身のリアクションのみが対象)
- 「利用可能なリアクションを先頭に表示する」設定の判定を、ロール制限・センシティブ・ローカル限定を考慮した実際のクリック可否と一致するように修正

## 投稿言語

- 投稿フォームに、投稿の言語(BCP 47言語タグ)を明示的に指定できるオプションを追加(Mastodon互換)。未指定の場合は表示言語の設定が既定値として使われ、ActivityPubでの連合にはAS2標準のcontentMapを使用(Mastodon/Akkoma互換)
- JUICE設定に、ホーム・ローカル・グローバルタイムラインへ表示する投稿を言語で絞り込める設定を追加(Mastodonの「フィルタする言語」を参考に実装。ただし全タイムラインに適用し、リノートもリノート元ノートの言語で判定する点が本家Mastodonと異なる)。言語未指定の投稿・自分自身の投稿は設定に関わらず常に表示。タイムライン画面の「…」メニューからも直接切り替え可能
- ノート検索・リアクション検索の高度な検索オプションに、投稿の言語での絞り込みを追加

## ノート検索の強化

- ノート検索画面に、AND/OR切り替え・除外ワード・添付ファイル有無・CW有無・返信有無・投票有無・公開範囲での絞り込みができる高度な検索オプションを追加(misskey-tempuraを参考に実装。ただし本家のクエリ構文をそのままパースする方式は構文エラーのリスクがあるため採用せず、従来通りパースを経由しない安全な方式のまま機能を拡張)

## AI生成物フラグ

- 投稿・添付Driveファイルに「AI Generated」フラグを追加。投稿画面のツールバー・投稿後のノートメニュー・ドライブのファイルメニューから切り替え可能
- ノート本体とファイル単位、それぞれ独立してバッジ表示(リノート・引用ノートでも元ノートのフラグが表示される)
- Drive画面に「AI生成物のみ表示」フィルタを追加
- 設定でAI生成物としてマークされた投稿の扱いを「なし・ミュート(折りたたみ)・ハードミュート(完全非表示)」から選択可能(タイムライン・通知・検索・ユーザーページに適用)
- `isAIGenerated`をActivityPubで連合(JUICE独自プロパティ、未対応サーバーからは無視される)

## ユーザーランキング

- 探索ページに投稿数(純粋リノートを除く)・リアクション数(受け取った数)それぞれの上位3人を表示する「ユーザーランキング」タブを追加。表示人数はコントロールパネルのJUICE設定から変更可能(既定3人)
- 集計はRedisの期間別カウンター方式(`FeaturedService`と同様のエポック基準ウィンドウ+前ウィンドウとのブレンド+TTL自然消滅)で行い、重いSQL集計を避けている。ActivityPub経由で受信したリモートユーザーの投稿・リアクションは対象外で、ローカルユーザーの活動のみを集計
- 集計期間はコントロールパネルのJUICE設定から変更可能(既定12時間)

## リレータイムライン

- 登録済みリレー経由で届いた公開ノートだけを表示する専用タイムラインを追加。コントロールパネルのJUICE設定で有効化した場合のみSocial/Globalの近くにタブとして表示される(既定は無効)
- 特定のリレーへの絞り込みフィルタ(複数選択可)をタイムラインヘッダーと設定の両方から操作可能。選択状態はアカウントに保存される
- 複数リレーから同じノートが届いた場合はfirst-writer-winsで1件のみ採用、過去ノートへの遡及付与はしない

## お知らせの投票機能

- お知らせに、既存のNote用Poll機能とは独立した専用の投票機能を追加(選択肢・複数選択可否・期限を新規作成時のみ設定可能、作成後の編集は不可)
- 個人宛てのお知らせには投票を付けられない
- 他のユーザーがリアクション・投票した際に、お知らせ一覧・個別ページの両方でリアルタイムに反映されるように(サーバー側は元々全クライアントへ通知を送信していたが、受け取って画面に反映するクライアント側の実装が無かった)

## LaTeX(数式)表示の復旧

- 本家Misskeyが2023年に削除したKaTeXによる数式レンダリングを、`\href`等の危険なコマンドを無効化する設定(`trust: false`)込みで復旧
- コントロールパネルのJUICE設定から無効化すると、これまで通り数式ソースがそのままコードとして表示される(既定は有効)
- 数式を深くネストさせた投稿(継続分数の延々ネスト等)でブラウザがフリーズ・クラッシュしないよう、ネスト深度・文字数が一定を超える場合はレンダリングを試みずテキスト表示にフォールバック

## お問い合わせフォーム

- ログイン不要(管理者設定で認証を必須にすることも可能)で、カテゴリ・返信方法(メールアドレス/Misskeyユーザー名)を選んで送信できるお問い合わせフォームを追加(misskey-tempuraを参考に実装)
- コントロールパネルに一覧・詳細画面を追加。ステータス管理(未対応/対応中/対応済み/クローズ)・管理者メモ・担当者割り当てが可能
- カテゴリはコントロールパネルのJUICE設定から追加・編集・並び替え・有効無効の切り替えが可能
- 受信時はSystemWebhook経由で通知できる(`receivedContactForm`イベント)

## モデレーション・管理者向け通知

- 絵文字申請・アバターデコレーション申請・承認式登録の新規申請があったとき、通報と同様にコントロールパネルへ警告バナーを表示。コントロールパネルを開いている間はadminストリーム経由でリアルタイムトースト通知も届く(通報についても今回初めてこの経路が実際に使われるようになった)
- SystemWebhookのイベント種別に`emojiRequestCreated`・`signupApplicationCreated`・`receivedContactForm`を追加
- 管理画面のモデレーションログに、承認式新規登録・絵文字申請・アバターデコレーション申請の承認/却下の詳細(対象ユーザー・却下理由等)を表示

## その他のUI/UX

- コントロールパネル・設定・プロフィールメニューに「JUICE」独自セクションを新設し、上記の各種設定をまとめている
- 本家Misskeyには無いJUICE独自の設定・タブ・メニュー項目に、視認性の高いオレンジ色の枠線スタイルの「JUICE」バッジを表示
- JUICE独自のAboutページ(`/about-juice`)を追加。開発者情報・ソースコード/ライセンス・参考にしたプロジェクト([CherryPick](https://github.com/kokonect-link/cherrypick))・独自機能の一覧を掲載
- サイト全体の既定アイコン(favicon・起動時スプラッシュ・PWAアイコン・インスタンスアイコン未設定時のフォールバック・システムメールのロゴ)をJUICE独自の雫アイコンに差し替え
- JUICE専用Aboutページのアイコンをクリックすると、オレンジの雨が降るイースターエッグ(実績「JUICEの雨」も解除される)
- Misskey Gamesに「盆栽を育てるゲーム」を追加。1日1回水をあげて育て、放置すると弱って退行する。関連実績3つ付き
- ジョブキューウィジェットで鳴らす音を、鳴らす/鳴らさないの切り替えだけでなく好きなサウンドに変更可能

## システムメールの多言語対応

- パスワードリセット・メールアドレス確認・新規登録確認・ログイン通知・アカウント削除通知・モデレーター不在警告・招待制切替通知・通報通知・承認/却下通知など、システムメール全体をユーザーごとの言語設定に対応
- 翻訳が無い言語では英語にフォールバック(`ja-JP.yml`以外はCrowdin管理のため直接編集できず、フォールバック文言はJUICE側で個別に保持)

## セキュリティ強化

- パスワード再設定申請・絵文字申請・承認式新規登録の審査状況確認(新規コード追加時)にcaptcha(hCaptcha/mCaptcha/reCAPTCHA/Turnstile/TestCaptcha)を適用
- ログイン試行の失敗をアカウント本人へ通知(アプリ内通知・メール)
- 絵文字・アバターデコレーション申請の承認/却下、承認式新規登録の承認/却下について、同一の対象に2件の審査リクエストがほぼ同時に処理されても片方しか成功しないよう排他制御を強化

## misskey-artからの移植

1.0リリース時にmisskey-artから移植した機能・修正。

- お知らせへのリアクション機能
- センシティブ画像のぼかし表示に関する複数の不具合修正
